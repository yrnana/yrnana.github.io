enableStrictSsl: false

yarnPath: .yarn/releases/yarn-3.6.3.cjs

nodeLinker: pnp
pnpMode: strict

supportedArchitectures:
  cpu:
    - current
    - x64
    - arm64
  libc:
    - current
    - glibc
    - musl
  os:
    - current
    - darwin
    - linux
